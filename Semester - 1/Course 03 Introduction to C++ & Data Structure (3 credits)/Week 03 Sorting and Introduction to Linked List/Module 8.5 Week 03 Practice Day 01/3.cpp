#include<bits/stdc++.h>
using namespace std;

int num_of_inversion(vector <int> a)
{
    if(a.size() == 2) {
        if(a[i] == 1)
            return;
    }
}

int main()
{
    int n;
    cin >> n;
    vector <int> a(n);
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }

}
